{
  "rules": {
    "devices": {
      "$deviceId": {
        "live": {
          ".read":  "auth != null",
          ".write": "auth != null"
        }
      }
    },
    "sessions": {
      "$deviceId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
