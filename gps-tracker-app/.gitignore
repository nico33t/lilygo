
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli.env

# ── Secrets & Firebase ───────────────────────────────────────────────────────
# Firebase iOS config (contains API keys). Source stays in project root.
# Never commit either copy — the root one is the template; use .env for tokens.
GoogleService-Info.plist
ios/GPSTracker/GoogleService-Info.plist
# Firebase Android config
google-services.json
android/app/google-services.json
# Environment variables (contain OAuth client IDs and API keys)
.env
.env.local
.env.production
# Firebase service-account keys (admin SDK)
*serviceAccountKey*.json
*firebase-adminsdk*.json
